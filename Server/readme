# 项目目录说明

`controller/`
负责对外提供 HTTP 接口，接收前端请求并返回处理结果。主要用于定义 GET、POST、PUT、DELETE 等接口。

`service/`
负责具体业务逻辑处理。Controller 接收到请求后，通常会调用 Service 完成实际业务操作。

`dto/`
用于定义前后端之间传输的数据结构，例如请求参数、返回结果等。DTO 主要承担数据传递作用，不负责具体业务逻辑。

`common/`
存放项目中多个模块都会使用的公共内容，例如统一返回结构、常量、公共工具等。

`config/`
存放 Spring Boot 的全局配置，包括接口拦截、统一响应处理、全局异常处理、跨域配置、请求头处理等。

`entity/`
用于定义与数据库表对应的数据实体。后续接入数据库后，数据库中的表通常会对应一个 Entity。

`repository/`
负责数据库访问，主要用于数据的查询、新增、修改和删除。一般由 Service 调用。

`inference/`
负责人工智能模型推理相关功能，例如 ONNX Runtime 模型加载、图片预处理、模型推理和结果后处理等。

整体调用关系可以理解为：

Controller → Service → Repository / Inference

其中 DTO 负责传输数据，Config 负责全局配置，Common 提供公共能力。
